# بررسی پروژه Nueno - سیستم ردیابی متقاضیان (ATS) منبع باز

## نمای کلی پروژه
**Nueno** یک سیستم ردیابی متقاضیان (Applicant Tracking System - ATS) منبع باز است که با استفاده از تکنولوژی‌های مدرن وب ساخته شده است. این سیستم به شرکت‌ها کمک می‌کند تا فرآیند استخدام و مدیریت متقاضیان کار را بهینه‌سازی کنند.

## تکنولوژی‌های استفاده شده

### Frontend
- **Next.js 12**: فریمورک React برای ساخت اپلیکیشن‌های Full-Stack
- **React 17**: کتابخانه UI
- **TypeScript**: برای type safety و بهبود کیفیت کد
- **Tailwind CSS**: برای استایل‌دهی سریع و responsive
- **Headless UI**: کامپوننت‌های UI آماده و accessible
- **React Query**: برای مدیریت state سرور و caching

### Backend
- **Next.js API Routes**: برای ساخت API endpoints
- **Prisma ORM**: برای تعامل با دیتابیس
- **PostgreSQL**: دیتابیس اصلی
- **NextAuth.js**: برای احراز هویت و مدیریت session
- **bcryptjs**: برای hash کردن رمزعبور

### ابزارهای توسعه
- **Jest**: برای تست‌نویسی
- **ESLint & Prettier**: برای کیفیت و فرمت کد
- **Husky & lint-staged**: برای pre-commit hooks
- **Docker & Docker Compose**: برای راه‌اندازی محیط توسعه

## امکانات اصلی سیستم

### 1. سیستم احراز هویت (Authentication)
- **ثبت نام (Signup)**: کاربران می‌توانند حساب کاربری جدید بسازند
- **ورود (Login)**: ورود امن با استفاده از NextAuth.js
- **مدیریت Session**: نگهداری امن session کاربران
- **رمزنگاری**: ذخیره امن رمزعبور با bcrypt

### 2. مدیریت شرکت‌ها (Company Management)
- هر کاربر به یک شرکت متصل است
- ایجاد خودکار شرکت هنگام ثبت نام کاربر اول
- جداسازی داده‌ها بین شرکت‌های مختلف (Multi-tenancy)

### 3. مدیریت موقعیت‌های شغلی (Job Management)
- **ایجاد شغل جدید**: امکان تعریف موقعیت‌های شغلی با عنوان و توضیحات
- **لیست مشاغل**: نمایش تمام موقعیت‌های شغلی شرکت
- **شناسه یکتا (UID)**: هر شغل دارای یک شناسه 8 کاراکتری یکتا است

### 4. فرم‌های درخواست سفارشی (Custom Application Forms)
سیستم امکان ساخت فرم‌های درخواست سفارشی برای هر موقعیت شغلی را فراهم می‌کند:

#### انواع فیلدها (Field Types):
- **SHORT_TEXT**: متن کوتاه (مثل نام، ایمیل)
- **LONG_TEXT**: متن بلند (مثل توضیحات)
- **CHECKBOX**: بله/خیر
- **SELECT**: انتخاب یک گزینه
- **MULTI_SELECT**: انتخاب چند گزینه

#### ویژگی‌های فیلدها:
- امکان تعیین فیلدهای اجباری (required)
- فیلدهای پیش‌فرض: نام، نام خانوادگی، ایمیل
- امکان اضافه کردن فیلدهای سفارشی
- قابلیت drag & drop برای مرتب‌سازی فیلدها (در UI طراحی شده)

### 5. رابط کاربری (UI/UX)
- **طراحی مدرن و Responsive**: استفاده از Tailwind CSS
- **Navigation Shell**: منوی ناوبری حرفه‌ای با:
  - بخش Jobs (مشاغل)
  - بخش Candidates (نامزدها)
  - Inbox (صندوق پیام)
  - Agenda (برنامه زمانی)
- **User Menu**: منوی کاربری با امکان خروج و تنظیمات
- **Loading States**: نمایش وضعیت بارگذاری
- **Form Validation**: اعتبارسنجی فرم‌ها

## ساختار دیتابیس (Database Schema)

### جداول اصلی:
1. **Company**: اطلاعات شرکت‌ها
2. **User**: اطلاعات کاربران (متصل به شرکت)
3. **Job**: موقعیت‌های شغلی (متصل به شرکت)
4. **Field**: فیلدهای فرم درخواست (متصل به شغل)
5. **FieldValue**: مقادیر وارد شده در فیلدها
6. **FieldChoice**: گزینه‌های فیلدهای انتخابی

## معماری پروژه

### ساختار پوشه‌ها:
```
/pages          # صفحات Next.js و API routes
/components     # کامپوننت‌های React
/business-logic # منطق تجاری (Business Layer)
/api-contracts  # قراردادهای API (Type definitions)
/helpers        # توابع کمکی
/prisma         # Schema و migrations دیتابیس
/tests          # تست‌های Jest
/styles         # فایل‌های CSS
```

### الگوی معماری:
- **Separation of Concerns**: جداسازی لایه‌های مختلف
- **Entity Pattern**: استفاده از کلاس‌ها برای business logic
- **Type Safety**: استفاده از TypeScript در تمام پروژه
- **API Contracts**: تعریف دقیق ورودی و خروجی APIها

## ویژگی‌های فنی برجسته

### 1. تست‌نویسی (Testing)
- پوشش تست برای business logic
- استفاده از Jest برای unit testing
- امکان اجرای تست با watch mode
- گزارش coverage

### 2. کیفیت کد (Code Quality)
- استفاده از ESLint و Prettier
- Pre-commit hooks با Husky
- Lint-staged برای بررسی فقط فایل‌های تغییر یافته
- Code Climate integration برای monitoring

### 3. محیط توسعه (Development Environment)
- Docker Compose برای راه‌اندازی سریع دیتابیس
- Hot Reload با Next.js
- Debug mode برای Node.js
- Gitpod support برای توسعه در cloud

## نقاط قوت پروژه
1. ✅ **معماری تمیز و منظم**
2. ✅ **Type Safety کامل با TypeScript**
3. ✅ **تست‌نویسی مناسب**
4. ✅ **استفاده از تکنولوژی‌های مدرن**
5. ✅ **UI/UX حرفه‌ای**
6. ✅ **قابلیت سفارشی‌سازی فرم‌ها**
7. ✅ **Multi-tenancy support**

## نواقص و محدودیت‌ها
1. ⚠️ **عدم تکمیل برخی features**: مثل ذخیره‌سازی فیلدهای فرم
2. ⚠️ **نبود مستندات کامل API**
3. ⚠️ **عدم پیاده‌سازی کامل بخش Candidates**
4. ⚠️ **نبود امکان مدیریت کاربران در یک شرکت**
5. ⚠️ **عدم پیاده‌سازی Inbox و Agenda**

## نتیجه‌گیری
Nueno یک پروژه **در حال توسعه** است که پایه‌های قوی برای یک سیستم ATS کامل دارد. با وجود اینکه هنوز برخی قابلیت‌ها تکمیل نشده‌اند، معماری و زیرساخت آن به خوبی طراحی شده و قابلیت توسعه و گسترش دارد. این پروژه برای کسانی که می‌خواهند یک ATS سفارشی بسازند یا در توسعه آن مشارکت کنند، نقطه شروع خوبی است.

## موارد استفاده (Use Cases)
- شرکت‌های کوچک و متوسط که نیاز به سیستم استخدام دارند
- توسعه‌دهندگانی که می‌خواهند یک ATS سفارشی بسازند
- پروژه‌های آموزشی برای یادگیری Next.js و Prisma
- مشارکت در پروژه‌های open source